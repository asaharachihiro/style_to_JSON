ここでは、JSON化したデザイントークンデータを
共通デザイントークンに変換し、JSONを生成します。

**---利用方法---**<br>
Style Dictionary で共通デザイントークンを生成

1.  ダウンロードしたJSONを tokens/source/ に配置
2.  CLIで変換を実行
    ```
    npm run build
    ```
3.  tokens/build/ に変換されたJSONが生成される
4.  source、buildのJSONはcommitしてOK
